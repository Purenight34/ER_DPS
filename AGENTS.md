# AGENTS.md

## 1. 프로젝트 목적

이 저장소는 **이터널 리턴** 이라는 게임의 API 키를 이용하여 데이터를 조회하고, 데이터 기반으로 캐릭터의 실전 콤보형 DPS를 **여러 조합**에 따라 비교하는 데미지 비교 조회 프로젝트이다.

여기서 **여러 조합**에 해당하는 조건은 아래와 같다.
1. 실험체의 종류
2. 실험체의 착용 장비
3. 실험체의 주/부 특성
4. 실험체의 레벨
5. 실험체의 스킬 레벨
6. 실험체의 무기/방어 등의 숙련도 시스템
7. 아군 실험체의 버프와 디버프 (후 순위 구현)
8. 적군 실험체의 버프와 디버프 (후 순위 구현)
9. 이외의 데미지에 있어 변수가 생길 수 있는 다른 조건들

이 데미지 비교 조회 프로젝트에서는 버전이 명시된 피해 공식과 효과 규칙에 따라 결과를 결정론적으로 계산한다. 게임 규칙의 근거는 공식 사이트와 해당 사이트가 제공하는 공식 API·문서를 우선한다. 공식 사이트가 제시하지 않은 자료나 규칙을 근거로 사용하려면 먼저 사용자에게 확인한다.

사용자가 제공한 실제 실험값을 바탕으로 미공개 규칙의 원인을 역으로 추론하고 가설을 제안할 수 있다. 추정 규칙은 공식 규칙과 구분하고, 사용자가 승인한 뒤에만 실제 계산에 반영한다. 이미 사용자가 승인한 임시 가정은 그 범위 안에서 적용하며 같은 내용을 반복 확인하지 않는다. Codex는 개발과 분석을 보조하되 검증되지 않은 가설을 공식 사실로 취급하거나 임의로 적용하지 않는다.

최종 결과물에서 사용자는 UI에서 다음 작업을 수행할 수 있어야 한다.

- 공격자와 방어자 캐릭터 선택
- 공격자와 방어자의 장비 및 특성 선택
- 선택 결과에 따른 영구 능력치 자동 계산
- 버프, 디버프, 스택, 중첩, 지속시간 및 조건부 효과가 반영된 전투 상태 계산
- 미리 정의한 스킬·기본 공격 콤보의 타임라인 제시
- 타임라인의 스킬 아이콘을 눌러 적중/빗나감 상태 변경
- 여러 틱이 있는 기술이나 장판같은 경우는 n번째 틱마다 적중 상태 설정
- 동일한 콤보와 적중 조건에서 여러 장비·특성 프리셋의 피해량 비교

이 계산기는 자동으로 최적 스킬 사이클을 찾거나 이론상 무한 지속 DPS를 계산하지 않는다.
`Q-기본 공격-W-기본 공격`, `Q-W-기본 공격`, `Q-R1-기본 공격-R2-기본 공격`처럼
**사용자가 정의한 고정 콤보 구간의 총 피해량과 콤보 DPS과 소요된 시간**만 계산한다.


## 2. API 호출과 네트워크
.env 파일에는 이터널리턴 공식 데이터 조회 API 키가 주어진다. 해당 키는 1초에 1번 조회할 수 있다. 단, 직접 접근하지 않는다.

제품의 데이터 동기화 및 전투 계산 실행 경로에서는 API 결과를 바탕으로 또 다른 AI 추론이나 모델 호출을 수행하지 않는다. 제품의 데미지 계산은 데이터와 명시된 규칙을 기반으로 결정론적으로 진행한다. 개발 과정에서 Codex가 사용자 제공 실험값과 계산 결과의 차이를 분석하고 규칙 가설을 제안하는 것은 허용하며, 그 절차는 code_fix 문서에 기록한다.

API 호출은 오직 게임 내 데이터를 불러오는데에만 사용되며, 타 유저의 전적 검색이나 게임 매치등의 기록 조회에는 사용되지 않는다.

해당 API를 통해 DPS 계산을 위해 필요한 캐릭터, 장비, 특성 등의 데이터를 정리하여 저장한다. 이후 API 호출은 오직 버전 변경에 따른 데이터 업데이트에만 사용된다. 

기본 환경 변수 이름:
- 'ER_API_KEY'

보안 규칙:
- API키는 직접 접근하지 않고, settings.py처럼 간접적인 호출을 통해서만 접근하여 노출 위험을 없앤다.
- 실제 API 키를 코드, 문서, 프리셋, fixture, 테스트 출력 또는 로그에 작성하지 않는다.
- API 키는 환경 변수 또는 저장소 밖의 승인된 비밀 저장소에서만 읽는다.
- `.env`의 전체 내용을 출력하지 않는다.
- `env`, `printenv`처럼 모든 환경 변수를 노출하는 명령을 실행하지 않는다.
- `Authorization`, `X-API-Key`, 쿠키 및 인증 query parameter를 로그에서 제거한다.
- UI에 API 키 입력값이나 실제 키 문자열을 표시하지 않는다.
- 저장된 게임 데이터 캐시에 인증 헤더를 포함하지 않는다.
- 키가 없으면 값을 추측하거나 소스에 임시 키를 넣지 않는다.
- 키가 없을 때는 fixture 기반 오프라인 테스트만 실행한다.

네트워크 규칙:

- 공식적으로 문서화된 읽기 endpoint만 호출한다.
- 기본적으로 `GET` 또는 `HEAD`만 사용한다.
- 공식 API가 조회를 위해 `POST`를 요구할 때만 명시적으로 허용한다.
- 데이터 변경, 계정 변경, 결제 또는 게임 플레이 조작 endpoint를 추가하지 않는다.
- 모든 요청에 timeout을 둔다.
- 무한 재시도를 구현하지 않는다.
- 429와 일시적인 5xx에만 제한된 횟수의 backoff를 적용한다.
- live API 테스트는 기본 테스트 명령에 포함하지 않는다.
- 실제 API가 없어도 핵심 기능 개발과 검증이 가능해야 한다.


## 3. 문서 종류와 폴더 구조
필요한 기본 문서는 아래와 같다.
- 게임 데이터를 API키로 받아오는 update_version
- update_version에서 받아온 데이터가 저장되는 폴더
- 데이터를 기반으로 연산하는 C++ 프로젝트
- 연산 결과를 UI로 출력하여 접근을 쉽게 하는 프로젝트
- 실제 실험값과 계산 결과의 차이, 역추론 가설, 검증 과정 및 사용자 승인 상태를 기록하는 code_fix 문서
- 그 외 추가가 필요한 사항은 해당 문서를 읽고 Codex에서 지적한다.

code_fix의 실험 및 역추론 규칙:

- 사용자가 실제 실험값을 제시하면 Codex가 계산 결과와 대조하여 치명타 보정, 피해 공식, 적용 순서 등 차이의 원인 후보를 역으로 추론한다.
- 게임 패치·데이터·공식 버전, 캐릭터·장비·특성·레벨·숙련도, 콤보·적중 상태·초기 전투 상태 등 재현에 필요한 조건과 관측값을 함께 기록한다. 빠진 조건은 추측으로 채우지 않는다.
- 각 가설의 계산 과정, 설명할 수 있는 실험값, 설명하지 못하는 실험값 및 가설을 구분할 추가 실험을 제시한다. 한 관측값에 맞는다는 이유만으로 규칙을 확정하지 않는다.
- 사용자 제공 실험값의 분석과 가설 제안은 진행할 수 있다. 새로운 비공식 자료의 근거 채택과 추정 규칙의 실제 계산 반영은 먼저 사용자에게 확인한다.
- 승인된 추정 규칙은 공식 규칙과 구분하여 근거, 적용 범위, 승인 내용 및 규칙 버전을 남기고 재현 테스트와 함께 반영한다. 이후 공식 데이터가 확보되면 대조하고 관련 문서와 테스트를 갱신한다.

## 4. 영구 능력치 계산

게임 내 데미지 계산에서 가장 우선되어야 할 것은 스탯이다.
스탯, 능력치 계산은 영구적인 능력치와 전투 도중의 일시 상태로 분리된다.

아래는 장비/특성 등 동일한 시뮬레이션 내에서 기본으로 주어지는 영구 능력치이다.

- 캐릭터 기본 능력치
- 레벨 성장 능력치
- 장비의 상시 능력치
- 특성의 상시 능력치
- 전투 시작 전 확정되는 영구 수정치

영구 능력치 계산 규칙:

- 영구 능력치 중, 원본 캐릭터·장비·특성 데이터는 불변 데이터로 취급한다.
- 합연산, 곱연산, 제한값 및 변환 순서는 공식 사이트가 제공하는 근거와 함께 문서화한다. 공식 근거가 없으면 먼저 사용자에게 확인하고, 실험값 기반 추정은 code_fix의 절차를 따른다.
- 정수 표기는 UI에 표시하는 능력치와 피해 수치에 대한 요구이며, 입력이나 내부 계산을 정수로 제한한다는 뜻이 아니다. 내부 소수 정밀도를 보존하고, 게임 계산의 절삭·반올림은 확인된 규칙에 따라서만 수행한다. 화면 표시를 위한 정수 변환은 표시 단계에서만 적용하고 계산 결과에 되먹이지 않는다.
- 치명타 확률과 치명타 피해량은 캐릭터 스탯에 주어진 값을 사용한다.
- 이전 치명타 발생 이력에 따른 확률 보정은 현재 확정된 규칙으로 취급하지 않는다. 공식 보정 데이터가 없으면 우선 스탯의 기본 치명타 확률만 사용하고 이력 보정을 적용하지 않는다. 이는 사용자가 승인한 임시 가정이며, 기본 확률을 사용한다는 이유로 확률을 무시하거나 모든 공격을 치명타로 처리하지 않는다.
- 치명타 보정은 공식 데이터가 확보되거나 사용자 실험값으로 추정한 규칙이 승인되면 별도로 반영한다. 확률 처리에서도 같은 입력의 결과를 재현할 수 있어야 하며, 실제 게임의 미공개 난수 규칙은 추측해 적용하지 않는다.
- UI 편의를 위해 공식을 바꾸지 않는다.
- 각 능력치가 어떤 소스에서 증가했는지 추적 가능한 구조를 선호한다.

영구 능력치 객체를 전투 도중 직접 변경하지 않는다.
버프와 디버프는 별도의 runtime 중에서 적용된다.

## 5. 실시간 효과 엔진

스킬, 장비 및 특성 효과는 시간 순서에 따라 동작하는 결정론적 이벤트 엔진으로 처리한다.

효과 정의는 필요한 범위에서 다음 정보를 명시한다.

- 공식 고유 ID
- 효과 출처
- 적용 대상
- 발동 이벤트
- 발동 조건
- 시작 시점
- 지속시간
- 최대 스택
- 현재 스택 변화량
- 중첩 방식
- 지속시간 갱신 방식
- 주기 효과 간격
- 능력치 수정치
- 추가 피해 또는 후속 이벤트
- 내부 재사용 대기시간
- 종료 또는 제거 조건

지원해야 하는 대표 효과:

- 공격자 버프와 방어자 디버프
- 스택 증가·감소와 최대 스택
- 동일 효과의 지속시간 갱신
- 독립적으로 만료되는 개별 스택
- 특정 스킬 적중 후 활성화
- 기본 공격 적중 시 발동
- 대상 체력, 거리, 스택 수 또는 선행 행동에 따른 조건부 효과
- 지속 피해 tick
- 일시적인 공격력, 방어력, 공격 속도, 피해 증폭 및 피해 감소

효과 처리 규칙:

- 모든 조건과 중첩 정책은 코드와 문서에 명시한다.
- 표시 이름 문자열 비교로 효과를 하드코딩하지 않는다.
- 서로 같은 시각의 이벤트는 안정적인 `sequence_index` 또는 공식 우선순위로 정렬한다.
- 효과 시작·갱신·만료 시점을 부동소수점 오차에 의존하지 않는다.
- 가능하면 시간은 정수 tick, 정수 밀리초 또는 정수 마이크로초로 표현한다.
- 매 시뮬레이션은 새로운 runtime state에서 시작한다.
- 프리셋 간 버프, 쿨다운, 스택 또는 체력 상태를 공유하지 않는다.

### 적중과 빗나감의 구분

스킬 사용과 스킬 적중은 같은 사건으로 취급하지 않는다.

스킬이 빗나간 경우에도 공식상 발생하는 다음 항목은 유지할 수 있다.

- 스킬 사용 자체
- 자원 소모
- 쿨다운 시작
- 시전 시 발동 효과

스킬이 빗나가면 기본적으로 다음 항목은 발생하지 않는다.

- 해당 적중 피해
- 적중 시 발동 효과
- 적중으로 얻는 스택
- 대상에게 적용되는 적중 기반 디버프

단, 실제 게임 공식이 다르면 해당 스킬 규칙에 예외를 명시하고 테스트한다.


## 6. 고정 콤보와 타임라인

콤보는 자동 생성된 최적 회전이 아니라 사용자가 정의한 명시적 이벤트 목록이다. 콤보는 캐릭터의 고유 스킬들과 평타의 타임 라인으로 이루어져 있다.

각 콤보 이벤트에는 최소한 다음 정보가 있어야 한다.

- 콤보 내 순번
- 발생 시각 또는 이전 이벤트와의 시간 간격
- 공격자와 대상
- 행동 종류
- 스킬 또는 기본 공격의 공식 ID
- 같은 스킬의 사용 회차
- 적중 여부
- 필요하면 다단 히트의 개별 impact ID
- 평타와 스킬의 선딜레이-후딜레이로 인한 실제 가능성 확인

엔진은 다음 행동을 하지 않는다.

- 목록에 없는 스킬의 사용
- 캐릭터 공격속도로 불가능한 속도의 평타 입력

자원, 쿨다운, 스택 또는 선행 조건 때문에 실행 불가능한 이벤트는
임의로 수정하지 말고 명시적인 validation 오류 또는 경고로 표시한다.

콤보는 계산 구간을 명시해야 한다.

- 시작 시각
- 종료 시각 또는 측정 구간 길이
- 전투 시작 시 보유한 스택·버프·자원

피해 집계와 사망 이후 계산은 다음 사용자 지정 규칙을 따른다.

- 보호막에 흡수된 피해와 체력에 가한 피해를 별도로 누적한다. 체력 피해는 남은 체력을 넘는 초과 피해도 포함한다.
- 총 유효 피해는 보호막 피해와 체력 피해의 합이며, 같은 피해를 두 항목에 중복 집계하지 않는다. 결과에는 두 항목을 각각 확인할 수 있게 표시한다.
- 정해진 콤보 구간 안에서는 대상 체력이 0이 되어도 남은 타격을 계속 계산한다. 사망을 이유로 콤보를 조기 종료하거나 목록에 없는 행동을 추가하지 않는다.
- 피해 공식과 조건부 효과가 참조하는 현재 체력은 계산상 잔여 체력의 최솟값을 0으로 제한한 값, 즉 `max(0, 계산상 잔여 체력)`을 사용한다. 사망 이후 타격은 현재 체력 0을 기준으로 계산한다.
- 초과 피해를 기록하는 계산상 잔여 체력은 0 아래의 음수까지 계속 감소시킨다. 이 음수 기록을 현재 체력 조건 판정에 그대로 넣지 않는다.
- 사망 이후 계산 방식은 게임의 실제 사망 처리를 재현한다는 의미가 아니라, 사용자가 지정한 콤보 피해 비교 규칙이다.

`combo_dps`는 위 기준의 총 유효 피해를 명시된 측정 구간으로 나눈 값이다.
이 값을 이론상 지속 DPS 또는 자동 공격 DPS라고 표시하지 않는다.

다단 히트 스킬은 내부적으로 각 impact를 구분한다.
개별 타격 토글을 지원한다면 각 impact마다 별도 ID를 사용한다.

## 7. 확정된 기술 원칙

- 제품 코드와 전투 계산 코어는 C++20 으로 작성한다.
- API 호출과 파일 출력 관련은 예외로 python으로 작성한다. C++로 하는 편이 간단하다면 그래도 좋다.
- 기존 저장소에 이미 언어 표준과 빌드 체계가 있으면 기존 설정을 우선한다.
- Python은 데이터 동기화, 변환, 검증처럼 Python이 더 적합한 보조 작업에 사용할 수 있다.
- Python을 제품의 전투 계산 코어에 대한 필수 런타임 의존성으로 만들지 않는다.
- API 호출은 게임 정보 조회용 읽기 작업으로만 사용한다.
- 공식에 없는 값, 순서, 조건 또는 반올림 규칙을 임의로 확정하거나 적용하지 않는다. 사용자 실험값으로 가설을 제안하는 것은 허용하되, 실제 계산에는 사용자가 승인한 추정 규칙과 명시적인 임시 가정만 반영하고 공식 규칙과 구분한다.
- 같은 입력, 같은 데이터 버전, 같은 콤보 정의는 항상 같은 결과를 만들어야 한다.

Python을 사용해도 되는 대표 작업:

- 공식 API의 읽기 전용 데이터 동기화 도구
- API 응답 구조 탐색
- JSON fixture 정리 및 비밀정보 제거
- 버전별 데이터 변환 또는 검증 스크립트
- 아이콘 및 정적 리소스 메타데이터 생성

Python으로 구현하지 말아야 하는 핵심 기능:

- 결정론적 계산
- 최종 능력치 계산 엔진
- 피해 공식
- 버프·디버프·스택 처리
- 전투 타임라인 실행
- 프리셋별 결과 비교
- UI가 호출하는 실제 시뮬레이션 경로

## 8. 타임라인 UI 동작

UI는 설정을 입력하고 전투 코어의 계산 결과를 별도 프로그램의 화면에 표시하는 데스크톱 실행 형태를 목표로 한다.

UI 구현 제안(아직 확정되지 않음): C++20 전투 코어와 Qt 6 Widgets를 사용하는 데스크톱 프로그램을 권장한다. 설정·표·프리셋·클릭 가능한 타임라인 중심의 화면과 기존 C++ 제품 코드 원칙에 맞는 선택이다. 웹 UI가 반드시 느리다고 가정하지 않으며, 실제 성능은 계산량과 화면 갱신 방식 등을 측정해 판단한다. 계산과 UI 처리를 분리하여 긴 계산이 화면 입력 처리를 막지 않도록 설계한다.

UI는 적어도 다음 영역을 분리해 제공하는 구조를 우선한다.

- 공격자 설정
- 방어자 설정
- 장비와 특성 설정
- 프리셋 목록
- 콤보 타임라인과 적중/비적중 설정
- 이벤트별 피해 내역
- 프리셋 비교 결과

필수 상호작용:

- 캐릭터, 장비 또는 특성을 변경하면 영구 능력치를 다시 계산한다.
- 전투 시나리오가 바뀌면 runtime state를 초기화하고 전체 콤보를 다시 계산한다.
- 타임라인에는 시간대별 스킬·기본 공격 아이콘을 표시한다.
- 각 아이콘은 안정적인 콤보 이벤트 ID와 연결한다.
- 아이콘을 누르면 해당 이벤트의 `Hit`/`Miss` 상태가 바뀐다.
- 상태 변경 후 모든 프리셋을 같은 조건으로 다시 계산한다.
- 빗나간 이벤트는 시각적으로 구분한다.
- 결과에는 총 피해량, 보호막 피해량, 체력 피해량, 콤보 DPS, 이벤트별 피해량 및 초과 피해가 반영된 계산상 잔여 체력을 표시한다. 능력치와 피해 수치는 UI에서 정수로 표시하며 내부 계산 정밀도는 유지한다.
- 사용한 패치·데이터·공식 버전을 표시한다.

UI 코드가 직접 피해량을 더하거나 스택을 수정하면 안 된다.
UI는 `SimulationScenario`와 동등한 입력을 만들고 전투 엔진의 결과를 표시한다.

## 9. 프리셋 비교

프리셋은 동일한 공격자·방어자·콤보 조건에서 장비와 특성 세팅 차이를 비교하기 위한 구조다.

각 프리셋은 필요한 범위에서 다음을 가진다.

- 프리셋 ID와 이름
- 공격자 장비
- 공격자 특성
- 방어자 장비 또는 방어 설정
- 방어자 특성
- 명시적인 추가 입력값

비교 모드의 공통 조건:

- 같은 콤보 정의
- 같은 이벤트 시각
- 같은 적중·빗나감 상태
- 같은 측정 구간
- 같은 게임 데이터와 공식 버전
- 같은 전투 시작 조건

프리셋 1을 기본 비교 기준으로 사용할 수 있어야 한다.
각 프리셋에 대해 다음 결과를 제공한다.

- 총 콤보 피해량
- 보호막 피해량과 체력 피해량의 별도 집계
- 콤보 DPS
- 이벤트별 피해량
- 초과 피해가 반영된 계산상 잔여 체력

프리셋이 콤보 실행 가능성을 바꾸는 경우에는 해당 이벤트를 임의로 제거하지 않는다.
비교 불가 또는 부분 유효 상태를 명확히 표시한다.


## 10. 작업 절차

코드를 수정하기 전에 다음 순서로 작업한다.

1. 관련 문서, 모델, 테스트 및 현재 데이터 버전을 확인한다.
2. 변경이 API, 영구 스탯, 효과 엔진, 타임라인, UI 중 어느 계층에 속하는지 정한다.
3. 공식에 없는 가정이 필요한지 확인한다.
4. 필요한 테스트를 먼저 추가하거나 재현 테스트를 만든다.
5. 책임 경계를 유지하면서 최소 범위로 구현한다.
6. 오프라인 테스트와 관련 정적 검사를 실행한다.
7. diff에서 비밀정보, 무관한 리팩터링 및 생성 파일을 확인한다.
8. 공식·효과·UI 동작이 달라졌다면 관련 문서를 함께 수정한다.

다음 행동을 피한다.

- UI에서 공식을 중복 구현
- 임의의 이론 DPS 계산 추가
- 자동 콤보 최적화 기능 추가
- 공식 근거 또는 실험값에 대한 사용자 승인 없이 상수 조정
- API 연결이 편하다는 이유로 combat 계층에서 직접 HTTP 호출
- 한 기능을 위해 관련 없는 대규모 리팩터링
- 테스트를 통과시키기 위해 공식 결과를 fixture에 맞춤


## 11. 문서 갱신 조건

다음 변경에는 관련 문서를 함께 수정한다.

- API endpoint, 인증 방식 또는 응답 필드 변경
- 게임 데이터 버전 변경
- 영구 능력치 계산 순서 변경
- 피해 공식 또는 방어 공식 변경
- 반올림 시점 변경
- 치명타 보정, 사용자 승인 추정 규칙 또는 임시 가정 변경
- 보호막·체력 피해의 집계 기준 또는 사망 이후 계산 기준 변경
- 버프, 디버프, 스택 및 조건부 효과 변경
- 콤보 이벤트 모델 변경
- 적중·빗나감 처리 변경
- 프리셋 비교 기준 변경
- UI의 핵심 상호작용 변경
- 새로운 환경 변수 또는 실행 명령 추가

`AGENTS.md`에는 매 작업에 적용되는 안정적인 원칙을 유지한다.
세부 캐릭터·스킬별 공식과 패치별 데이터는 별도 문서나 버전 데이터 파일에 둔다.


## 12. 완료 조건

작업은 다음 조건을 만족해야 완료로 본다.

- 요청한 기능이 책임에 맞는 계층에 구현되었다.
- 전투 계산 경로에 AI 또는 LLM 의존성이 추가되지 않았다.
- API 호출은 읽기 전용이며 비밀정보가 노출되지 않았다.
- 같은 입력에 같은 결과가 나온다.
- 공식과 적용 순서가 코드·문서·테스트에서 일치한다.
- 적중·빗나감 변경이 타임라인과 모든 프리셋 결과에 반영된다.
- 프리셋별 계산이 독립된 runtime state에서 수행된다.
- 관련 단위 테스트와 회귀 테스트가 통과한다.
- 기본 테스트는 live API 없이 통과한다.
- 패치, 데이터 및 공식 버전을 결과에서 추적할 수 있다.
- 실행하지 못한 검증이 있다면 이유를 숨기지 않고 보고한다.


## 13. 최종 보고 형식

작업 후 다음 내용을 간단히 보고한다.

- 변경한 기능
- 주요 변경 파일
- 공식 또는 데이터 버전
- 추가하거나 변경한 계산 규칙
- 실행한 테스트와 결과
- 실행하지 않은 테스트와 이유
- 새로 생긴 가정 또는 아직 해결되지 않은 규칙
- API, 보안, 수치 정확성 또는 비교 가능성 관련 주의사항
