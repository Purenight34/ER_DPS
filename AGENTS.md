# AGENTS.md

## 1. 프로젝트 목적

이 저장소는 **C++ 중심의 게임 콤보 피해량/DPS 계산기**다.

공식 게임 API에서 캐릭터, 장비, 특성, 스킬 및 능력치 데이터를 **읽기 전용으로 조회**하고,
버전이 명시된 공식 피해 공식과 효과 규칙에 따라 결과를 결정론적으로 계산한다.

이 프로젝트의 런타임 계산에는 AI, LLM, 추론 모델을 사용하지 않는다.
Codex는 개발을 보조할 뿐이며, 게임 데이터 해석이나 피해 공식 자체를 임의로 만들어서는 안 된다.

사용자는 UI에서 다음 작업을 수행할 수 있어야 한다.

- 공격자와 방어자 캐릭터 선택
- 공격자와 방어자의 장비 및 특성 선택
- 선택 결과에 따른 영구 능력치 자동 계산
- 버프, 디버프, 스택, 중첩, 지속시간 및 조건부 효과가 반영된 전투 상태 계산
- 미리 정의한 스킬·기본 공격 콤보의 시간대별 적중 확인
- 타임라인의 스킬 아이콘을 눌러 적중/빗나감 상태 변경
- 동일한 콤보와 적중 조건에서 여러 장비·특성 프리셋의 피해량 비교

이 계산기는 자동으로 최적 스킬 사이클을 찾거나 이론상 무한 지속 DPS를 계산하지 않는다.
`Q-기본 공격-W-기본 공격`, `Q-W-기본 공격`, `Q-R1-기본 공격-R2-기본 공격`처럼
**사용자가 정의한 고정 콤보 구간의 총 피해량과 콤보 DPS**만 계산한다.


## 2. 확정된 기술 원칙

- 제품 코드와 전투 계산 코어는 C++로 작성한다.
- 새 프로젝트라면 기본 언어 표준은 C++20을 사용한다.
- 기존 저장소에 이미 언어 표준과 빌드 체계가 있으면 기존 설정을 우선한다.
- Python은 데이터 동기화, 변환, 검증처럼 Python이 더 적합한 보조 작업에 사용할 수 있다.
- Python을 제품의 전투 계산 코어에 대한 필수 런타임 의존성으로 만들지 않는다.
- 공식 API 동기화를 Python 도구로 구현한다면, 결과를 버전이 명시된 정규화 스냅샷으로 저장하고 C++ 애플리케이션이 해당 스냅샷을 읽게 한다.
- 같은 기능을 C++ API 클라이언트와 Python 동기화 도구 양쪽에 중복 구현하지 말고 저장소에서 선택한 경로를 따른다.
- API 호출은 게임 정보 조회용 읽기 작업으로만 사용한다.
- 공식에 없는 값, 순서, 조건 또는 반올림 규칙을 추측하지 않는다.
- 같은 입력, 같은 데이터 버전, 같은 콤보 정의는 항상 같은 결과를 만들어야 한다.

Python을 사용해도 되는 대표 작업:

- 공식 API의 읽기 전용 데이터 동기화 도구
- API 응답 구조 탐색
- JSON fixture 정리 및 비밀정보 제거
- 버전별 데이터 변환 또는 검증 스크립트
- 아이콘 및 정적 리소스 메타데이터 생성
- C++ 계산 결과를 검증하는 일회성 참조 계산

Python으로 구현하지 말아야 하는 핵심 기능:

- 최종 능력치 계산 엔진
- 피해 공식
- 버프·디버프·스택 처리
- 전투 타임라인 실행
- 프리셋별 결과 비교
- UI가 호출하는 실제 시뮬레이션 경로


## 3. 아직 결정되지 않은 항목

다음 항목은 저장소에 이미 선택된 기술이 있는지 먼저 확인한다.
선택되어 있지 않다면 작업 요청에서 필요한 경우에만 후보와 근거를 제시하고,
관련 없는 작업 중 임의로 새 프레임워크를 도입하지 않는다.

- UI 프레임워크
- HTTP 클라이언트 라이브러리
- JSON 라이브러리
- 테스트 프레임워크
- 패키지 관리자
- 지원 운영체제 범위
- API 제공 게임사와 정확한 endpoint

기존 의존성으로 해결할 수 있는 작업에 새 production dependency를 추가하지 않는다.


## 4. 정보와 공식의 우선순위

작업 전에 관련 문서와 테스트를 확인한다.
문서가 아직 존재하지 않는다면 관련 기능을 처음 구현할 때 함께 생성하되, 문서가 없다는 이유로 규칙을 추측하지 않는다.

1. 공식 게임 API 문서 및 공식 게임 규칙
2. 패치 버전이 기록된 공식 데이터 스냅샷
3. `docs/API_CONTRACT.md`
4. `docs/DAMAGE_FORMULAS.md`
5. `docs/EFFECT_RULES.md`
6. `docs/COMBO_MODEL.md`
7. `docs/UI_SPEC.md`
8. 회귀 테스트와 기존 코드

공식 문서와 코드가 충돌하면 기존 코드를 그대로 정답으로 취급하지 않는다.
충돌 내용을 명시하고, 공식 근거와 버전 정보를 기준으로 코드·문서·테스트를 함께 수정한다.

공식 자료만으로 규칙을 확정할 수 없으면 다음 중 하나로 처리한다.

- 지원되지 않는 규칙으로 명시적으로 실패
- 문서에 `UNRESOLVED`로 기록
- 사용자에게 필요한 공식 근거가 없음을 보고

관찰값만으로 공식을 역추정하거나 임의 보간한 값을 production 규칙으로 추가하지 않는다.


## 5. 권장 저장소 구조와 책임

저장소의 기존 구조가 있다면 이름을 억지로 바꾸지 말고 아래 책임 경계를 유지한다.

```text
src/
  app/          애플리케이션 조립, 진입점, 의존성 연결
  api/          공식 API 읽기, 인증, timeout, retry, 오류 변환
  data/         API DTO, 캐시, 버전 스냅샷, domain 변환
  domain/       캐릭터, 장비, 특성, 스킬, 스탯 등의 순수 모델
  combat/       능력치 해석, 효과 엔진, 타임라인, 피해 계산
  presets/      빌드 프리셋과 비교 시나리오
  ui/           화면, 입력, 아이콘 상호작용, 결과 표시

tools/
  python/       오프라인 데이터 처리 및 검증 도구

tests/
  unit/
  integration/
  fixtures/

docs/
  API_CONTRACT.md
  DAMAGE_FORMULAS.md
  EFFECT_RULES.md
  COMBO_MODEL.md
  UI_SPEC.md
```

계층 규칙:

- 제품 코드에서는 `api/`만 네트워크와 API 키에 접근한다.
- 별도 Python 데이터 동기화 도구가 있다면 `tools/python/`의 승인된 동기화 모듈만 읽기 전용 네트워크와 API 키에 접근한다.
- `data/`는 외부 응답을 내부 domain model로 정규화한다.
- `combat/`는 네트워크, 환경 변수, UI 위젯을 참조하지 않는다.
- `ui/`에는 피해 공식과 스택 계산을 작성하지 않는다.
- UI는 시나리오를 구성해 전투 엔진을 호출하고 결과만 표시한다.
- 외부 API의 raw JSON을 전투 엔진에 직접 전달하지 않는다.
- API 필드명 변경은 adapter/data 계층에서 흡수한다.
- 전투 엔진은 테스트에서 API 없이 독립 실행할 수 있어야 한다.


## 6. 읽기 전용 API와 비밀정보

API 키는 공식 게임 정보를 읽어오기 위한 인증 수단일 뿐이다.
API 결과를 바탕으로 또 다른 AI 추론이나 모델 호출을 수행하지 않는다.

기본 환경 변수 이름:

- `GAME_API_BASE_URL`
- `GAME_API_KEY`
- `GAME_API_TIMEOUT_SECONDS`
- `LIVE_API_TESTS`

보안 규칙:

- 실제 API 키를 코드, 문서, 프리셋, fixture, 테스트 출력 또는 로그에 작성하지 않는다.
- API 키는 환경 변수 또는 저장소 밖의 승인된 비밀 저장소에서만 읽는다.
- `.env`의 전체 내용을 출력하지 않는다.
- `env`, `printenv`처럼 모든 환경 변수를 노출하는 명령을 실행하지 않는다.
- `Authorization`, `X-API-Key`, 쿠키 및 인증 query parameter를 로그에서 제거한다.
- UI에 API 키 입력값이나 실제 키 문자열을 표시하지 않는다.
- 저장된 게임 데이터 캐시에 인증 헤더를 포함하지 않는다.
- 키가 없으면 값을 추측하거나 소스에 임시 키를 넣지 않는다.
- 키가 없을 때는 fixture 기반 오프라인 테스트만 실행한다.

네트워크 규칙:

- 공식적으로 문서화된 읽기 endpoint만 호출한다.
- 기본적으로 `GET` 또는 `HEAD`만 사용한다.
- 공식 API가 조회를 위해 `POST`를 요구할 때만 명시적으로 허용한다.
- 데이터 변경, 계정 변경, 결제 또는 게임 플레이 조작 endpoint를 추가하지 않는다.
- 모든 요청에 timeout을 둔다.
- 무한 재시도를 구현하지 않는다.
- 429와 일시적인 5xx에만 제한된 횟수의 backoff를 적용한다.
- live API 테스트는 기본 테스트 명령에 포함하지 않는다.
- 실제 API가 없어도 핵심 기능 개발과 검증이 가능해야 한다.


## 7. 게임 데이터 스냅샷과 버전

API에서 가져온 데이터는 가능한 한 버전이 명시된 스냅샷으로 정규화한다.

각 스냅샷에는 가능한 범위에서 다음 메타데이터를 보존한다.

- 게임 패치 버전
- API 또는 데이터 버전
- 조회 시각
- 데이터 제공자
- 내부 스키마 버전

결과 화면과 내보낸 결과에는 사용한 데이터 버전과 공식 버전을 표시한다.
서로 다른 패치의 데이터와 공식을 조용히 혼합하지 않는다.

식별에는 표시 이름보다 공식 고유 ID를 우선한다.
표시 이름은 번역이나 패치로 변경될 수 있으므로 로직 분기에 사용하지 않는다.


## 8. 영구 능력치 계산

UI에서 캐릭터, 레벨, 장비 및 특성을 선택하면 공격자와 방어자의 영구 능력치를 자동 계산한다.

영구 능력치는 다음과 같은 전투 도중의 일시 상태와 분리한다.

- 캐릭터 기본 능력치
- 레벨 성장 능력치
- 장비의 상시 능력치
- 특성의 상시 능력치
- 전투 시작 전 확정되는 영구 수정치

영구 능력치 계산 규칙:

- 원본 캐릭터·장비·특성 데이터는 불변 데이터로 취급한다.
- 계산 결과는 별도의 `ResolvedStats` 또는 동등한 값 객체로 만든다.
- 합연산, 곱연산, 제한값 및 변환 순서는 공식 문서에 정의한다.
- 퍼센트, 배율, 퍼센트 포인트를 같은 값으로 취급하지 않는다.
- 단위를 이름 또는 타입으로 드러낸다.
- UI 편의를 위해 공식을 바꾸지 않는다.
- 각 능력치가 어떤 소스에서 증가했는지 추적 가능한 구조를 선호한다.

영구 능력치 객체를 전투 도중 직접 변경하지 않는다.
버프와 디버프는 별도의 runtime state에서 적용한다.


## 9. 실시간 효과 엔진

스킬, 장비 및 특성 효과는 시간 순서에 따라 동작하는 결정론적 이벤트 엔진으로 처리한다.

효과 정의는 필요한 범위에서 다음 정보를 명시한다.

- 공식 고유 ID
- 효과 출처
- 적용 대상
- 발동 이벤트
- 발동 조건
- 시작 시점
- 지속시간
- 최대 스택
- 현재 스택 변화량
- 중첩 방식
- 지속시간 갱신 방식
- 주기 효과 간격
- 능력치 수정치
- 추가 피해 또는 후속 이벤트
- 내부 재사용 대기시간
- 종료 또는 제거 조건

지원해야 하는 대표 효과:

- 공격자 버프와 방어자 디버프
- 스택 증가·감소와 최대 스택
- 동일 효과의 지속시간 갱신
- 독립적으로 만료되는 개별 스택
- 특정 스킬 적중 후 활성화
- 기본 공격 적중 시 발동
- 대상 체력, 거리, 스택 수 또는 선행 행동에 따른 조건부 효과
- 지속 피해 tick
- 일시적인 공격력, 방어력, 공격 속도, 피해 증폭 및 피해 감소

효과 처리 규칙:

- 모든 조건과 중첩 정책은 코드와 문서에 명시한다.
- 표시 이름 문자열 비교로 효과를 하드코딩하지 않는다.
- 서로 같은 시각의 이벤트는 안정적인 `sequence_index` 또는 공식 우선순위로 정렬한다.
- 효과 시작·갱신·만료 시점을 부동소수점 오차에 의존하지 않는다.
- 가능하면 시간은 정수 tick, 정수 밀리초 또는 정수 마이크로초로 표현한다.
- 매 시뮬레이션은 새로운 runtime state에서 시작한다.
- 프리셋 간 버프, 쿨다운, 스택 또는 체력 상태를 공유하지 않는다.

### 적중과 빗나감의 구분

스킬 사용과 스킬 적중은 같은 사건으로 취급하지 않는다.

스킬이 빗나간 경우에도 공식상 발생하는 다음 항목은 유지할 수 있다.

- 스킬 사용 자체
- 자원 소모
- 쿨다운 시작
- 시전 시 발동 효과

스킬이 빗나가면 기본적으로 다음 항목은 발생하지 않는다.

- 해당 적중 피해
- 적중 시 발동 효과
- 적중으로 얻는 스택
- 대상에게 적용되는 적중 기반 디버프

단, 실제 게임 공식이 다르면 해당 스킬 규칙에 예외를 명시하고 테스트한다.


## 10. 고정 콤보와 타임라인

콤보는 자동 생성된 최적 회전이 아니라 사용자가 정의한 명시적 이벤트 목록이다.

각 콤보 이벤트에는 최소한 다음 정보가 있어야 한다.

- 안정적인 이벤트 ID
- 콤보 내 순번
- 발생 시각 또는 이전 이벤트와의 시간 간격
- 공격자와 대상
- 행동 종류
- 스킬 또는 기본 공격의 공식 ID
- 같은 스킬의 사용 회차
- 적중 여부
- 필요하면 다단 히트의 개별 impact ID

엔진은 다음 행동을 하지 않는다.

- 쿨다운이 돌아올 때 스킬을 자동 사용
- 빈 시간에 기본 공격을 자동 삽입
- 가장 높은 DPS 콤보 탐색
- 사용자가 적은 콤보 순서 자동 변경
- 잘못된 타임라인을 조용히 뒤로 밀어 유효하게 만들기

자원, 쿨다운, 스택 또는 선행 조건 때문에 실행 불가능한 이벤트는
임의로 수정하지 말고 명시적인 validation 오류 또는 경고로 표시한다.

콤보는 계산 구간을 명시해야 한다.

- 시작 시각
- 종료 시각 또는 측정 구간 길이
- 전투 시작 시 보유한 스택·버프·자원

`combo_dps`는 해당 콤보의 총 유효 피해를 명시된 측정 구간으로 나눈 값이다.
이 값을 이론상 지속 DPS 또는 자동 공격 DPS라고 표시하지 않는다.

다단 히트 스킬은 내부적으로 각 impact를 구분한다.
UI는 하나의 스킬 아이콘으로 묶어 표시할 수 있지만,
아이콘을 빗나감으로 바꾸면 그 행동에 속한 impact를 일관되게 비활성화해야 한다.
개별 타격 토글을 지원한다면 각 impact마다 별도 ID를 사용한다.


## 11. 타임라인 UI 동작

UI는 적어도 다음 영역을 분리해 제공하는 구조를 우선한다.

- 공격자 설정
- 방어자 설정
- 장비와 특성 설정
- 프리셋 목록
- 콤보 타임라인
- 이벤트별 피해 내역
- 프리셋 비교 결과

필수 상호작용:

- 캐릭터, 장비 또는 특성을 변경하면 영구 능력치를 다시 계산한다.
- 전투 시나리오가 바뀌면 runtime state를 초기화하고 전체 콤보를 다시 계산한다.
- 타임라인에는 시간대별 스킬·기본 공격 아이콘을 표시한다.
- 각 아이콘은 안정적인 콤보 이벤트 ID와 연결한다.
- 아이콘을 누르면 해당 이벤트의 `Hit`/`Miss` 상태가 바뀐다.
- 상태 변경 후 모든 프리셋을 같은 조건으로 다시 계산한다.
- 빗나간 이벤트는 시각적으로 구분한다.
- 결과에는 총 피해량, 콤보 DPS, 이벤트별 피해량을 표시한다.
- 사용한 패치·데이터·공식 버전을 표시한다.

UI 코드가 직접 피해량을 더하거나 스택을 수정하면 안 된다.
UI는 `SimulationScenario`와 동등한 입력을 만들고 전투 엔진의 결과를 표시한다.


## 12. 프리셋 비교

프리셋은 동일한 공격자·방어자·콤보 조건에서 장비와 특성 세팅 차이를 비교하기 위한 구조다.

각 프리셋은 필요한 범위에서 다음을 가진다.

- 프리셋 ID와 이름
- 공격자 장비
- 공격자 특성
- 방어자 장비 또는 방어 설정
- 방어자 특성
- 명시적인 추가 입력값

비교 모드의 공통 조건:

- 같은 콤보 정의
- 같은 이벤트 시각
- 같은 적중·빗나감 상태
- 같은 측정 구간
- 같은 게임 데이터와 공식 버전
- 같은 전투 시작 조건

프리셋 1을 기본 비교 기준으로 사용할 수 있어야 한다.
각 프리셋에 대해 다음 결과를 제공한다.

- 총 콤보 피해량
- 콤보 DPS
- 이벤트별 피해량
- 프리셋 1 대비 절대 차이
- 프리셋 1 대비 백분율 차이

프리셋이 콤보 실행 가능성을 바꾸는 경우에는 해당 이벤트를 임의로 제거하지 않는다.
비교 불가 또는 부분 유효 상태를 명확히 표시한다.


## 13. 수치 계산과 결정성

- 피해 계산에는 충분한 정밀도의 수치형을 사용한다.
- 공식에서 요구하는 단계 외에는 중간 반올림을 추가하지 않는다.
- UI 표시용 반올림과 내부 계산용 값을 분리한다.
- 반올림, 내림, 올림 및 정수 변환 시점을 문서화한다.
- 0으로 나누기, NaN, infinity, 음수 지속시간 및 잘못된 스택 값을 검증한다.
- 부동소수점 테스트에는 프로젝트에서 정한 tolerance를 사용한다.
- 같은 시각의 이벤트 순서는 항상 동일해야 한다.
- 결과가 컨테이너 순회 순서나 해시 순서에 따라 달라지면 안 된다.

확률 요소를 자동으로 Monte Carlo 시뮬레이션하지 않는다.
치명타나 확률 발동이 필요하면 시나리오에서 다음 중 하나를 명시한다.

- 고정 결과
- 고정 seed를 사용하는 명시적 확률 모드
- 공식에 근거한 기대값 모드

선택한 모드를 결과와 문서에 표시하며, 서로 다른 모드를 조용히 혼합하지 않는다.


## 14. 권장 핵심 모델

기존 명명 규칙이 없다면 아래 개념을 기준으로 설계한다.
정확한 클래스명은 프로젝트 스타일에 맞게 조정할 수 있다.

- `GameDataSnapshot`
- `CharacterDefinition`
- `ItemDefinition`
- `TraitDefinition`
- `SkillDefinition`
- `CombatantBuild`
- `ResolvedStats`
- `RuntimeCombatState`
- `EffectDefinition`
- `EffectInstance`
- `ComboDefinition`
- `TimelineEvent`
- `SimulationScenario`
- `BuildPreset`
- `SimulationResult`
- `PresetComparisonResult`

참조 데이터, 계산된 영구 능력치, 전투 중 상태를 하나의 변경 가능한 객체에 섞지 않는다.


## 15. 빌드와 검증 명령

저장소에 이미 정의된 명령이 있으면 해당 명령을 우선한다.
새 CMake 프로젝트라면 기본 명령은 다음과 같다.

```bash
cmake -S . -B build -DBUILD_TESTING=ON
cmake --build build --config Debug
ctest --test-dir build -C Debug --output-on-failure
```

다음 도구는 설정 파일이 저장소에 존재할 때만 실행한다.

```bash
clang-format
clang-tidy
cmake-format
```

Python 도구가 존재할 때는 해당 디렉터리의 안내를 따른다.
Python 테스트 체계가 이미 구성되어 있다면 예시는 다음과 같다.

```bash
python -m pytest tools/python/tests
```

존재하지 않는 lint, formatter 또는 test framework를 단순히 명령을 맞추기 위해 추가하지 않는다.


## 16. 테스트 규칙

일반 테스트는 네트워크 없이 실행되어야 한다.

### API 및 데이터 테스트

- 정상 응답과 누락 필드를 테스트한다.
- nullable 필드를 테스트한다.
- 401, 403, 404, 429 및 일시적 5xx를 테스트한다.
- timeout과 제한된 retry를 테스트한다.
- 패치 및 스키마 버전 불일치를 테스트한다.
- fixture에는 API 키, 인증 헤더 및 개인 식별 정보를 포함하지 않는다.

### 영구 능력치 테스트

- 기본 능력치만 있는 경우
- 레벨 성장 적용
- 장비 여러 개 적용
- 특성 적용
- 퍼센트와 고정 수치의 적용 순서
- 상한·하한 및 변환 규칙
- 공격자와 방어자 설정의 독립성

### 효과 엔진 테스트

- 버프 시작과 만료
- 디버프 시작과 만료
- 최대 스택
- 지속시간 갱신
- 독립 스택 만료
- 조건 충족과 미충족
- 내부 쿨다운
- 지속 피해 tick 경계
- 같은 시각 이벤트 순서
- 스킬 빗나감 시 on-hit 효과 미발동
- 스킬 빗나감 시 on-cast 효과 유지

### 콤보 테스트

- 손으로 계산 가능한 짧은 콤보
- 기본 공격과 스킬 혼합
- 다단 히트
- 동일 스킬 여러 번 사용
- 이벤트 적중/빗나감 전환
- 측정 구간 변경
- 실행 불가능한 쿨다운 또는 자원 조건
- 콤보 종료 직전과 직후의 효과 만료

### 프리셋 비교 테스트

- 같은 콤보에 서로 다른 장비 적용
- 같은 적중 마스크가 모든 프리셋에 적용되는지 확인
- 프리셋 1 대비 절대·백분율 차이
- 프리셋 간 runtime state 누수 방지
- 비교 불가능한 프리셋의 오류 표시

공식 또는 적용 순서를 바꾸는 변경에는 반드시 회귀 테스트를 추가한다.


## 17. 작업 절차

코드를 수정하기 전에 다음 순서로 작업한다.

1. 관련 문서, 모델, 테스트 및 현재 데이터 버전을 확인한다.
2. 변경이 API, 영구 스탯, 효과 엔진, 타임라인, UI 중 어느 계층에 속하는지 정한다.
3. 공식에 없는 가정이 필요한지 확인한다.
4. 필요한 테스트를 먼저 추가하거나 재현 테스트를 만든다.
5. 책임 경계를 유지하면서 최소 범위로 구현한다.
6. 오프라인 테스트와 관련 정적 검사를 실행한다.
7. diff에서 비밀정보, 무관한 리팩터링 및 생성 파일을 확인한다.
8. 공식·효과·UI 동작이 달라졌다면 관련 문서를 함께 수정한다.

다음 행동을 피한다.

- UI에서 공식을 중복 구현
- 임의의 이론 DPS 계산 추가
- 자동 콤보 최적화 기능 추가
- 공식 근거 없이 상수 조정
- API 연결이 편하다는 이유로 combat 계층에서 직접 HTTP 호출
- 한 기능을 위해 관련 없는 대규모 리팩터링
- 테스트를 통과시키기 위해 공식 결과를 fixture에 맞춤


## 18. 문서 갱신 조건

다음 변경에는 관련 문서를 함께 수정한다.

- API endpoint, 인증 방식 또는 응답 필드 변경
- 게임 데이터 버전 변경
- 영구 능력치 계산 순서 변경
- 피해 공식 또는 방어 공식 변경
- 반올림 시점 변경
- 버프, 디버프, 스택 및 조건부 효과 변경
- 콤보 이벤트 모델 변경
- 적중·빗나감 처리 변경
- 프리셋 비교 기준 변경
- UI의 핵심 상호작용 변경
- 새로운 환경 변수 또는 실행 명령 추가

`AGENTS.md`에는 매 작업에 적용되는 안정적인 원칙을 유지한다.
세부 캐릭터·스킬별 공식과 패치별 데이터는 별도 문서나 버전 데이터 파일에 둔다.


## 19. 완료 조건

작업은 다음 조건을 만족해야 완료로 본다.

- 요청한 기능이 책임에 맞는 계층에 구현되었다.
- 전투 계산 경로에 AI 또는 LLM 의존성이 추가되지 않았다.
- API 호출은 읽기 전용이며 비밀정보가 노출되지 않았다.
- 같은 입력에 같은 결과가 나온다.
- 공식과 적용 순서가 코드·문서·테스트에서 일치한다.
- 적중·빗나감 변경이 타임라인과 모든 프리셋 결과에 반영된다.
- 프리셋별 계산이 독립된 runtime state에서 수행된다.
- 관련 단위 테스트와 회귀 테스트가 통과한다.
- 기본 테스트는 live API 없이 통과한다.
- 패치, 데이터 및 공식 버전을 결과에서 추적할 수 있다.
- 실행하지 못한 검증이 있다면 이유를 숨기지 않고 보고한다.


## 20. 최종 보고 형식

작업 후 다음 내용을 간단히 보고한다.

- 변경한 기능
- 주요 변경 파일
- 공식 또는 데이터 버전
- 추가하거나 변경한 계산 규칙
- 실행한 테스트와 결과
- 실행하지 않은 테스트와 이유
- 새로 생긴 가정 또는 아직 해결되지 않은 규칙
- API, 보안, 수치 정확성 또는 비교 가능성 관련 주의사항
